/**
 * WALIMATUL — Hybrid Editorial Scoped Font Definitions
 *
 * Uses CSS variables mapped to standard system and typography tokens.
 * Zero external network fetch during build.
 */

export const fontVariablesClass = "font-sans";
